# Local-only credentials for running the bot on your machine.
# Copy this file to ".env" and fill in your real values.
# .env is gitignored — never commit your real credentials.

WG_EMAIL=your-email@example.com
WG_PASSWORD=your-password
